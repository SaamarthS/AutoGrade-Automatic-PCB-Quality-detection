# Direct dependencies only — Streamlit Cloud resolves transitive deps automatically.
# Pruned from full requirements.txt to avoid build timeouts.

streamlit>=1.35.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
reportlab>=4.0.0
openai>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Cloud logging
gspread>=6.0.0
google-auth>=2.20.0
